#!/usr/bin/python3
"""read_file function"""


def read_file(filename=""):
    """read a file

    Keyword Arguments:
        filename {str} -- file name to be readed (default: {""})
    """
    with open(filename, mode="r", encoding="utf-8") as f:
        pass
